# Data & storage (Phase 1)
sqlalchemy>=2.0,<3
# PostgreSQL driver (optional; only needed when using postgresql:// URLs)
# psycopg2-binary>=2.9

# Parquet bars (Step 1.2)
pyarrow>=14.0
pandas>=2.0

# Alpha research (Phase 2) — cointegration, Kalman, OU
numpy>=1.24
scipy>=1.10
statsmodels>=0.14

# Ingestion (Step 1.4) — Alpha Vantage
requests>=2.28

# Testing
pytest>=7.0
pytest-asyncio>=0.21
